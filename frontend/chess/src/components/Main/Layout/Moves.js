import React from 'react'
import { connect } from 'react-redux'

function Moves() {
    return (
        <h1>moves</h1>
    )
}

const mapStateToProps = (state) => ({
    
})

const actionCreator = {
    
}

export default connect(mapStateToProps, actionCreator)(Moves)
